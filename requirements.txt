# Core graph framework
langgraph>=0.2.0
langchain>=0.2.0
langchain-openai>=0.1.0        # swap for langchain-anthropic if preferred

# Data
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
pyarrow>=14.0.0                # for parquet snapshots

# Vector store
chromadb>=0.5.0

# API layer (Day 8+)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Fine-tuning (Day 10-12)
# Uncomment when ready -- heavy deps, don't install on Day 1
# torch>=2.2.0
# transformers>=4.40.0
# peft>=0.10.0                 # LoRA/QLoRA
# datasets>=2.19.0
# bitsandbytes>=0.43.0         # 4-bit quantization for QLoRA

# Experiment tracking (optional)
# mlflow>=2.12.0

# Dev / test
pytest>=8.0.0
python-dotenv>=1.0.0
typing_extensions>=4.9.0
